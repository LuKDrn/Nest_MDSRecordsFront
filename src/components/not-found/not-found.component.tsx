import React, { FunctionComponent, useEffect, ReactNode } from 'react';
import Layout from '../layouts/layout.component';



const NotFound: FunctionComponent = () => (
    <Layout>
        <h1>Are you losted ?</h1>
        <h2>There's nothing to do here, it's just boring.</h2>
        
  </Layout>
  );

export default NotFound;
